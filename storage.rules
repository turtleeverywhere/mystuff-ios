rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Photos live under users/{uid}/... — owner-only access.
    match /users/{uid}/{allPaths=**} {
      allow read, write: if request.auth != null && request.auth.uid == uid;
    }
  }
}
